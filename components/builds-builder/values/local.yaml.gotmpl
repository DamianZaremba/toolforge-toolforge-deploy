chartVersion: 0.0.139-20260112134719-8aef69fd
chartRepository: toolsbeta

withLocalVolume: true
repository:
  username: "robot$local-image-builder"
  password: "Dummyr0botpass"
  # You can override this by exporting the env var HARBOR_IP before running ./depoy.sh
  url: http://{{ exec "../../helpers/get_harbor_ip.sh" (list) }}

imagesSource:
  runImage:
    toolforge: "toolsbeta-harbor.wmcloud.org/toolforge/heroku-runner:22-cnb"

dockerconfig: '{"insecure-registries": ["http://{{ exec "../../helpers/get_harbor_ip.sh" (list) }}"]}'

kubeVersion: ">=1.21"
