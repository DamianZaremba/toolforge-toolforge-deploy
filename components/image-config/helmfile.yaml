---
environments:
  local:
    values:
      # needed to use the chartVersion
      - values/local.yaml*
  toolsbeta:
    values:
      # needed to use the chartVersion
      - values/toolsbeta.yaml*
  tools:
    values:
      # needed to use the chartVersion
      - values/tools.yaml*

---
repositories:
  - name: toolsbeta
    url: toolsbeta-harbor.wmcloud.org/toolforge
    oci: true
  - name: tools
    url: tools-harbor.wmcloud.org/toolforge
    oci: true

releases:
  - name: image-config
    chart: "{{ .Values.chartRepository }}/image-config"
    version: "{{ .Values.chartVersion }}"
    namespace: tf-public
    createNamespace: true
    values:
      - values/{{ .Environment.Name }}.yaml*
