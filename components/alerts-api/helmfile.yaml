---
environments:
  local:
    values:
      - values/local.yaml*
  toolsbeta:
    values:
      - values/toolsbeta.yaml*
  tools:
    values:
      - values/tools.yaml*

---
repositories:
  - name: toolsbeta
    url: toolsbeta-harbor.wmcloud.org/toolforge
    oci: true
  - name: tools
    url: tools-harbor.wmcloud.org/toolforge
    oci: true

releases:
  - name: alerts-api
    chart: "{{ .Values.chartRepository }}/alerts-api"
    version: "{{ .Values.chartVersion }}"
    namespace: alerts-api
    createNamespace: true
    values:
      - values/{{ .Environment.Name }}.yaml*
