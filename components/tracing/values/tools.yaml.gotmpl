loki:
  chartVersion: 6.30.1
  storage:
    bucketNames:
      chunks: tools-loki-tracing-chunks
      ruler: tools-loki-tracing-ruler
      admin: tools-loki-tracing-admin

lokiTracing:
  htpasswd: {{ exec "../../../helpers/get_secret.sh" (list "lokiTracingHtpasswd") | quote }}

gateway:
  ingress:
    tls:
      - hosts:
        - loki-tracing-write.loki.svc.tools.local
    hosts:
      - host: loki-tracing-write.loki.svc.tools.local
        paths:
          - path: /
            pathType: Prefix
