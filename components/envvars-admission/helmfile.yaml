---
environments:
  local:
    values:
      # needed to use the chartVersion
      - values/local.yaml*
  toolsbeta:
    values:
      # needed to use the chartVersion
      - values/toolsbeta.yaml*
  tools:
    values:
      # needed to use the chartVersion
      - values/tools.yaml*

---
repositories:
  - name: toolsbeta
    url: toolsbeta-harbor.wmcloud.org/toolforge
    oci: true
  - name: tools
    url: tools-harbor.wmcloud.org/toolforge
    oci: true

releases:
  - name: envvars-admission
    chart: "{{ .Values.chartRepository }}/envvars-admission"
    version: "{{ .Values.chartVersion }}"
    namespace: envvars-admission
    createNamespace: true
    values:
      - values/{{ .Environment.Name }}.yaml*
