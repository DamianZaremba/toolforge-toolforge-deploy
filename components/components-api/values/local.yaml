chartVersion: 0.0.206-20260817120711-a9c79b26
# Use tools for now, as we are developing too quick without deploying
# and the toolsbeta images are getting deleted
chartRepository: tools
replicas: 1
config:
  debug: true
  toolforge_api_url: "https://api-gateway.api-gateway.svc"
  verify_toolforge_api_cert: false
certificates:
  internalClusterDomain: cluster.local
image:
  pullPolicy: Always
resources:
  limits:
    memory: ~
    cpu: ~
  requests:
    memory: ~
    cpu: ~
